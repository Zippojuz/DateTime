import TitleScreen from './screens/TitleScreen.jsx'

// Milestone 0: a single screen. Routing between screens (Creation, WorldMap,
// Dialogue, ...) arrives with Milestone 1.
export default function App() {
  return <TitleScreen />
}
